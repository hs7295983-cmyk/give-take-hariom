# Cart Quantity Replacement Files

Upload these files to your GitHub project root and replace the old files:

- `app.js`
- `styles.css`

This update adds `+` and `-` quantity buttons in the cart section and keeps cart totals updated.
